apiVersion: v1
type: Opaque
kind: Secret
metadata:
  name: ${APP_NAME}-postgresql-backup-credentials  
  namespace: ${APP_NAMESPACE}
data:
  ACCESS_KEY_ID: ${SECRET_MINIO_ACCESS_KEY_BASE64}
  ACCESS_SECRET_KEY: ${SECRET_MINIO_SECRET_KEY_BASE64}
stringData:
  REGION: ${MINIO_REGION}
