apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kasm
  namespace: flux-system
spec:
  path: "./kubernetes/apps/kasm/app"
  postBuild:
    substitute:
      APP_NAME: kasm
      APP_NAMESPACE: apps
