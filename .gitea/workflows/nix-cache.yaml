name: Build and Push Nix Cache

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      # - 'systems/**'
      - 'flake.nix'
      - 'flake.lock'

jobs:
  build-and-push:
    uses: actions/nix-cache/.gitea/workflows/system.yaml@v6
    secrets: inherit
    with:
      TARGET: "supermicro-k3s"
